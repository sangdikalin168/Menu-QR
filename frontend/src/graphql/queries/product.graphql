query Products {
  products {
    id
    name
    price
    categories {
      id
      name
    }
    image
    enabled
  }
}

query Product($id: ID!) {
  product(id: $id) {
    id
    name
    price
    categories {
      id
      name
    }
    image
    enabled
  }
}
